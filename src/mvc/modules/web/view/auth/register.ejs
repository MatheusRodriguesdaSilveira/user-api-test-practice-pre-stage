<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="/public/css/output.css" rel="stylesheet">
</head>
<body class="bg-gray-50 flex items-center justify-center min-h-screen font-sans">
    
    <div class="max-w-md w-full mx-auto p-8 bg-white border border-gray-100 rounded-xl shadow-2xl transition duration-500">
        
        <h2 class="text-3xl font-extrabold text-center text-indigo-700 mb-8">
            <%= title %>
        </h2>

        <% if (error) { %>
            <div class="bg-red-100 text-red-700 p-3 rounded-lg border border-red-400 mb-4 font-medium text-sm">
                ERRO: <%= error %>
            </div>
        <% } %>

        <form method="POST" action="/web/register" class="space-y-6"> 
            
            <div>
                <label for="name" class="block text-sm font-medium text-gray-700 mb-1">Nome Completo:</label>
                <input type="text" id="name" name="name" required
                       class="mt-1 block w-full px-4 py-2 border border-gray-300 rounded-lg shadow-sm focus:ring-indigo-500 focus:border-indigo-500 transition duration-150"
                       placeholder="Seu nome completo">
            </div>
            
            <div>
                <label for="email" class="block text-sm font-medium text-gray-700 mb-1">Email:</label>
                <input type="email" id="email" name="email" required
                       class="mt-1 block w-full px-4 py-2 border border-gray-300 rounded-lg shadow-sm focus:ring-indigo-500 focus:border-indigo-500 transition duration-150"
                       placeholder="seu.email@admin.com">
            </div>
            
            <div>
                <label for="password" class="block text-sm font-medium text-gray-700 mb-1">Senha:</label>
                <input type="password" id="password" name="password" required
                       class="mt-1 block w-full px-4 py-2 border border-gray-300 rounded-lg shadow-sm focus:ring-indigo-500 focus:border-indigo-500 transition duration-150"
                       placeholder="Crie uma senha forte">
            </div>
            
            <button type="submit" class="w-full py-2.5 px-4 rounded-lg shadow-md text-base font-semibold text-white bg-green-600 hover:bg-green-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-green-500 transition duration-150">
                Cadastrar
            </button>
        </form>

        <p class="text-center mt-6 text-sm text-gray-500">
            Já tem uma conta? <a href="/web/login" class="font-medium text-indigo-600 hover:text-indigo-500 transition duration-150">Fazer Login</a>
        </p>
        
        <p class="text-center mt-4 text-sm text-gray-400">
            <a href="/" class="hover:text-gray-600 transition duration-150">Home</a> 
        </p>
    </div>
</body>
</html>